# 📋 Учебный Агент — План разработки

> Этот файл обновляется после КАЖДОЙ сессии. Перед новым чатом — читается первым делом.

---

## 🏷️ Текущая стабильная версия
**v0.5-stable** (коммит: `04bdeac` — последний рабочий пуш)
- ✅ Вебинары: VTT-перехват, chunking, DeepSeek V4 Flash
- ✅ Конспекты: создание в Obsidian (Second Brain)
- ✅ Термины: авто-создание из TERMS + нормализация имён
- ✅ GitHub: автопуш через `push.sh`
- ❌ Файлы (PDF/DOCX/PPTX): парсер есть, но не интегрирован стабильно
- ❌ Дисциплины: `get_program_disciplines` ломался после патча `domcontentloaded`, требуется откат

---

## 📁 Структура проекта (целевая)

```
Учебный_Агент/
├── 📜 run_agent.py              # Главный оркестратор (только вызовы, не логика)
├── 📜 generate_summary.py       # Генерация конспектов (OpenRouter)
├── 📜 push.sh                   # Автопуш на GitHub
├── 📜 TODO.md                   # Этот файл
├── 📜 requirements.txt          # Зависимости
├── 📜 .env                      # Ключи API (в .gitignore!)
├── 📁 Промпты/
│   └── system.txt               # Промпт для модели
├── 📁 Скрейперы/
│   ├── __init__.py
│   └── netology_scraper.py      # Главный скрейпер (Нетология)
├── 📁 Генераторы/
│   ├── __init__.py
│   └── summary_generator.py     # Логика chunking + вызов API
├── 📁 Утилиты/
│   ├── __init__.py
│   ├── file_utils.py            # safe_filename, ensure_dirs
│   └── term_utils.py            # Парсинг TERMS, создание заметок
├── 📁 Тесты/
│   └── test_scraper.py          # Тесты на реальных URL
└── 📁 Данные/
    ├── netology_cookies.json
    └── debug/                   # HTML-дампы при ошибках
```

---

## ✅ Выполнено (лог)

| Дата | Что сделано | Результат | Коммит |
|------|-------------|-----------|--------|
| 14.05 | VTT-перехват вебинаров | ✅ Работает | — |
| 14.05 | Chunking + Laguna M.1 | ✅ Работал, но обрезал вывод | — |
| 14.05 | Переход на DeepSeek V4 Flash | ✅ Качество выше, 20K chunks | — |
| 14.05 | Авто-создание терминов | ✅ Работает, дубли убраны | — |
| 14.05 | Нормализация имён файлов | ✅ safe_filename с обрезкой | — |
| 14.05 | push.sh | ✅ Автопуш работает | — |
| 14.05 | Парсер PDF/DOCX/PPTX | ⚠️ Есть в netology_scraper.py, но не стабилен | — |
| 15.05 | Патч `domcontentloaded` в `get_program_disciplines` | ❌ Сломал поиск дисциплин | — |
| 15.05 | Попытка интеграции файлов в run_agent.py | ❌ Сломало create_term_files | — |

---

## 🔧 Активные проблемы

1. **`get_program_disciplines` не находит дисциплины**
   - Причина: заменили `networkidle` на `domcontentloaded`, React не успевает отрисовать
   - Решение: откатить `netology_scraper.py` до рабочей версии
   - Статус: 🔴 Блокер

2. **Файлы >100K символов (учебники)**
   - Причина: free-tier DeepSeek не выдерживает 10+ chunks подряд
   - Решение: заглушки для учебников + ручная обработка
   - Статус: 🟡 В ожидании

3. **Бардак в `run_agent.py` после патчей**
   - Причина: `sed` и `replace` оставляли артефакты (wiki_links в create_term_files)
   - Решение: полная перезапись файла целиком, не патчами
   - Статус: 🟢 Исправлено

4. **Мусор в репозитории**
   - Причина: тестовые файлы, дебаг-дампы, дубли скрейперов
   - Решение: удалить мусор, русифицировать структуру
   - Статус: 🟡 В процессе

---

## 🎯 Следующие шаги

### Сейчас (v0.6)
- [ ] Откатить `netology_scraper.py` до рабочей версии (`networkidle`)
- [ ] Удалить мусор (debug-файлы, дубли, тестовые данные)
- [ ] Переименовать папки на русский (src/parsers → Скрейперы)
- [ ] Протестировать полный прогон вебинаров ИЭУ (9 вебинаров)
- [ ] Сделать `git tag v0.5-stable`

### v0.7 (Файлы — отдельный эпик)
- [ ] Вынести `_extract_file_text` в `Скрейперы/file_parser.py`
- [ ] Создать `Генераторы/summary_generator.py` (chunking отдельно)
- [ ] Интегрировать через импорт в `run_agent.py`, не патчами
- [ ] Тест на коротких PDF (<50K) из раздела «Презентации»

### v0.8 (Полный прогон)
- [ ] Прогон всех дисциплин программы
- [ ] Проверка Second Brain на целостность связей
- [ ] README для портфолио (GitHub)

---

## 📝 Правила работы (для агента)

1. **Никаких `sed` и `replace()` в рабочем коде.** Только полная перезапись файла через `cat >`.
2. **Новый функционал — новый файл.** Потом импорт в `run_agent.py`.
3. **Коммиты только на русском.**
4. **После каждой сессии обновить TODO.md.**
5. **Перед новым функционалом — `git tag`.** Если ломается — откат.
6. **Не запускать агент до проверки.** Сначала `python -m py_compile`, потом тест.
